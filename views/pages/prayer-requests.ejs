<!DOCTYPE html>
<html lang="en">

<head>
    <title>Prayer Requests</title>
    <%- include('../layout/head') %>
    <link rel="stylesheet" href="../../styles/list.css">
</head>

<body>
    <%- include('../layout/header') %>
    <main>
        <div class="all">
            <div>
                <h1><i class="fas fa-praying-hands"></i></h1>
                <h1>All Prayer Requests</h1>
        </div>
        <!-- TODO: Handle singluar vs plural when rendering -->
        <h6>There are currently <%=prayers.length %> <% if(prayers.length > 1){%> prayer<%} else { %> prayers<%}%> saved in the database</h6>
        <ul class= "object-list">
        <% prayers.forEach(prayer => { %>
            <li>
            <h3><%= prayer.prayer %></h3>
            <div class="buttons">
                <form action="/prayer/<%=prayer.id%>" method="POST">
                <input type="hidden" name="_method" value="DELETE">
                <input type="submit" value="Delete">
                </form>
            </div>
            </li>
        <% }) %>
        </ul>
    </div>
    </main>
    <%- include('../layout/footer') %>
</body>

</html>